## Enums 

* Data types of related constants that can be used to represent a set of predefined values.

=> Instead of using arbitrary numbers or strings, enums group these values under a single, meaningful names.

=> One source of truth, also we can loop through the enum values.
